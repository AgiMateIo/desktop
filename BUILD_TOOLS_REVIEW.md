# Сравнительный анализ инструментов сборки Python-приложений

## Контекст проекта System Agent

**Архитектура:**
- PySide6 (Qt) приложение для системного трея
- Асинхронная архитектура: asyncio + qasync
- **Динамическая загрузка плагинов** через `importlib.util.spec_from_file_location()`
- macOS menu bar app (требуется `LSUIElement: True`)
- Кросс-платформенность: macOS, Windows, Linux

**Текущий setup:**
- PyInstaller с `.spec` файлом
- Размер бандла: ~80-100 MB
- Работающая система сборки

---

## 1. PyInstaller (текущий)

### Описание
Стандартный инструмент упаковки Python-приложений. Собирает Python-интерпретатор, зависимости и код в единый исполняемый файл или папку.

### Как работает
1. Анализирует импорты и зависимости
2. Копирует Python runtime + библиотеки в `dist/`
3. Создает загрузчик (bootloader) который распаковывает и запускает код

### Преимущества для System Agent

| Аспект | Оценка | Комментарий |
|--------|--------|-------------|
| **Динамические плагины** | ✅ Отлично | Полная поддержка `importlib`, `.py` файлы включаются как data |
| **PySide6 поддержка** | ✅ Хорошо | Работает из коробки, есть hook |
| **macOS LSUIElement** | ✅ Встроено | `info_plist={'LSUIElement': True}` в .spec |
| **Размер бандла** | ⚠️ Средне | ~80-100 MB (Qt доминирует) |
| **Время сборки** | ✅ Быстро | ~1-2 минуты |
| **Время старта** | ⚠️ Медленно | 2-30 сек (распаковка в temp) |
| **Конфигурация** | ✅ Гибко | `.spec` файл на Python |
| **Зрелость** | ✅ Высокая | 10+ лет, активная поддержка |

### Недостатки

1. **Медленный запуск** — распаковка в temp директорию занимает 2-30 сек
2. **Ложные срабатывания антивирусов** на Windows (bootloader)
3. **Нет нативных инсталляторов** — нужны внешние инструменты (NSIS, Inno Setup)

### Вердикт: ✅ РЕКОМЕНДУЕТСЯ

**Причина:** Единственный инструмент с полной поддержкой динамической загрузки плагинов из `.py` файлов. Проверенное решение, работающее в текущем проекте.

---

## 2. Briefcase (BeeWare)

### Описание
Инструмент от BeeWare для создания нативных приложений. Фокус на "правильной" структуре Python-пакетов и нативных форматах дистрибуции.

### Как работает
1. Требует структуру Python-пакета (`myapp/__init__.py`)
2. Создает нативные app bundles (`.app`, `.exe`, `.deb`)
3. Использует `importlib.resources` вместо файловых путей

### Преимущества

| Аспект | Оценка | Комментарий |
|--------|--------|-------------|
| **Нативные инсталляторы** | ✅ Встроено | .dmg, .msi, .deb, .rpm из коробки |
| **Конфигурация** | ✅ Единая | Все в `pyproject.toml` |
| **macOS LSUIElement** | ✅ Есть | `"info.LSUIElement" = true` |
| **Документация** | ✅ Хорошая | Официальная от BeeWare |

### Критические недостатки для System Agent

| Проблема | Серьёзность | Описание |
|----------|-------------|----------|
| **Требует реструктуризации** | 🔴 Высокая | Нужно переместить все в пакет `system_agent/` |
| **Изменение path resolution** | 🔴 Высокая | `sys._MEIPASS` → `importlib.resources` |
| **Плагины** | 🔴 Высокая | Динамическая загрузка через `Path.iterdir()` несовместима |
| **Размер бандла** | 🔴 Критичный | **~500 MB** (в 5 раз больше текущего!) |
| **Нет cross-compilation** | 🟡 Средняя | Сборка только на целевой ОС |

### Требуемые изменения

```
# Текущая структура
system-agent/
├── main.py
├── core/
├── plugins/
└── assets/

# Требуемая для Briefcase
system-agent/
├── system_agent/        # ← Переместить ВСЕ сюда
│   ├── __init__.py
│   ├── __main__.py
│   ├── core/
│   ├── plugins/
│   └── assets/
└── pyproject.toml
```

**Переписать `paths.py`:**
```python
# Было (PyInstaller)
if getattr(sys, 'frozen', False):
    return Path(sys._MEIPASS) / 'plugins'

# Станет (Briefcase)
from importlib.resources import files, as_file
with as_file(files('system_agent.plugins')) as path:
    return path
```

### Вердикт: ❌ НЕ РЕКОМЕНДУЕТСЯ

**Причины:**
1. Размер бандла увеличится в 5 раз (~500 MB)
2. Требуется существенная реструктуризация проекта
3. Система плагинов требует переписывания
4. ROI низкий — выгоды не оправдывают затраты

---

## 3. Nuitka

### Описание
Python-to-C компилятор. Транслирует Python код в C, затем компилирует в нативный бинарник.

### Как работает
1. Парсит Python AST
2. Генерирует эквивалентный C код
3. Компилирует через gcc/clang/MSVC
4. Линкует с Python runtime

### Преимущества

| Аспект | Оценка | Комментарий |
|--------|--------|-------------|
| **Время старта** | ✅ Отлично | **~0.07 сек** (vs 2-30 сек PyInstaller) |
| **Защита кода** | ✅ Есть | Скомпилированный C сложнее реверсить |
| **PySide6 поддержка** | ✅ Официально | Qt рекомендует Nuitka |
| **Производительность** | ✅ Лучше | Нативный код быстрее bytecode |
| **Поддержка** | ✅ Активная | Регулярные релизы |

### Критические недостатки для System Agent

| Проблема | Серьёзность | Описание |
|----------|-------------|----------|
| **Динамические плагины** | 🔴 Критичный | Nuitka 1.4+ **запрещает** `.py` файлы в data |
| **macOS LSUIElement** | 🟡 Средняя | Нет встроенной опции, нужен post-build скрипт |
| **Время сборки** | 🟡 Средняя | 10-100x дольше PyInstaller (компиляция C) |
| **Размер бандла** | ⚠️ Не улучшится | Qt библиотеки доминируют |
| **Сложность отладки** | 🟡 Средняя | Ошибки компиляции сложнее диагностировать |

### Проблема с плагинами (детально)

Текущий код загрузки плагинов:
```python
# plugin_manager.py
spec = importlib.util.spec_from_file_location(
    f"plugins.{plugin_dir.parent.name}.{plugin_dir.name}",
    plugin_file  # plugins/triggers/visual_buttons/plugin.py
)
module = importlib.util.module_from_spec(spec)
spec.loader.exec_module(module)
```

**Почему Nuitka несовместим:**
1. `--include-data-dir` больше не принимает `.py` файлы (Nuitka 1.4+)
2. Варианты:
   - Скомпилировать плагины → теряется динамичность
   - Использовать `--nofollow-import-to=plugins` → сложная конфигурация
   - Переписать систему discovery → большие затраты

### Вердикт: ⚠️ ОГРАНИЧЕННО ПОДХОДИТ

**Причины:**
1. Динамическая загрузка плагинов — фундаментальная несовместимость
2. Время старта улучшится, но это не критично для tray-приложения
3. Потребуется переписать plugin_manager.py

**Когда пересмотреть:**
- Если откажетесь от динамических плагинов
- Если время старта станет критичным
- Если потребуется защита исходного кода

---

## 4. PyOxidizer

### Описание
Rust-based инструмент для создания standalone Python приложений. Встраивает Python в единый исполняемый файл.

### ❌ ПРОЕКТ ЗАБРОШЕН

**Статус на январь 2023:**
> "The future of PyOxidizer is uncertain, possibly dead."
> — Gregory Szorc (автор), GitHub Issue #741

### Критические проблемы

| Проблема | Серьёзность | Описание |
|----------|-------------|----------|
| **Не поддерживает Python 3.11+** | 🔴 Критичный | Только Python 3.8, 3.9, 3.10 |
| **Заброшен** | 🔴 Критичный | Последний релиз 0.24.0, нет активной разработки |
| **Динамические импорты** | 🔴 Критичный | Static importer несовместим с runtime plugin discovery |
| **`__file__` недоступен** | 🔴 Критичный | Сломает всю систему путей |
| **PySide6 поддержка** | 🔴 Плохая | Extension modules имеют проблемы загрузки |

### Из документации PyOxidizer:
> "Plugin systems load Python modules outside the context of the normal import mechanism"
> — Официальная документация, раздел о несовместимостях

### Вердикт: ❌ НЕ ИСПОЛЬЗОВАТЬ

**Причины:**
1. Проект заброшен — нет будущего
2. Не поддерживает Python 3.11+ — несовместим с современным PySide6
3. Фундаментально несовместим с плагинной архитектурой

---

## Сводная таблица сравнения

| Критерий | PyInstaller | Briefcase | Nuitka | PyOxidizer |
|----------|-------------|-----------|--------|------------|
| **Статус проекта** | ✅ Активен | ✅ Активен | ✅ Активен | ❌ Заброшен |
| **Python 3.11+** | ✅ Да | ✅ Да | ✅ Да | ❌ Нет |
| **Динамические плагины** | ✅ Работает | ⚠️ Сложно | ❌ Проблемы | ❌ Невозможно |
| **Размер бандла** | ~100 MB | ~500 MB | ~100 MB | N/A |
| **Время старта** | 2-30 сек | ? | ~0.07 сек | N/A |
| **Время сборки** | ~2 мин | ~5 мин | ~20+ мин | N/A |
| **macOS LSUIElement** | ✅ Встроено | ✅ Есть | ⚠️ Скрипт | N/A |
| **Нативные инсталляторы** | ❌ Внешние | ✅ Встроено | ❌ Внешние | N/A |
| **Реструктуризация** | Не нужна | Обязательна | Не нужна | N/A |
| **Конфигурация** | .spec (Python) | pyproject.toml | CLI + nuitka.toml | N/A |
| **Сложность миграции** | — | 🔴 Высокая | 🟡 Средняя | N/A |

---

## Итоговый вывод

### Рекомендация: Остаться на PyInstaller ✅

**Обоснование:**

1. **Динамическая загрузка плагинов** — ключевая архитектурная особенность System Agent. PyInstaller — единственный инструмент с полной поддержкой.

2. **Размер бандла не уменьшится** — Qt библиотеки занимают ~60-80 MB независимо от инструмента. Briefcase увеличит размер в 5 раз.

3. **Работающее решение** — текущий setup протестирован и стабилен. Миграция создаёт риски без существенных выгод.

4. **ROI отрицательный** — затраты на миграцию (1-4 дня) не оправданы улучшениями.

### Матрица решений

| Если нужно... | Решение |
|---------------|---------|
| Уменьшить размер бандла | Добавить `excludes` в .spec (tkinter, unittest, etc.) |
| Автоматизировать сборку | CI/CD с GitHub Actions |
| Создавать инсталляторы | NSIS/Inno Setup (Windows), create-dmg (macOS) |
| Ускорить запуск | Nuitka, но с переписыванием plugin system |
| Защитить код | Nuitka (компиляция в C) |

### Дальнейшие действия

1. **Сейчас:** Закоммитить текущие изменения (task GC fix, datetime fix)
2. **Рекомендуется:** Добавить CI/CD для автоматизации сборки
3. **По желанию:** Оптимизировать размер через excludes в .spec
4. **Отложить:** Миграцию на другие инструменты до изменения требований
